package main

import "fmt"

func main()  {
	//Testing go Commands...
	//fmt.Printf("Another program in %s!") -- To see how go vet runs, uncomment this line.
	fmt.Printf("Another program in %s!", "Go")

	/*
	Execute some programs in your terminal!
	go env -- Gives environment information.
	go version -- Shows the go version.
	go vet file.go -- Get the line where there are errors.
	go run file.go -- Compile and run a go file.
	go build file.go -- Build an .exe of the file.
	go get github.com.br/go-sql-driver -- Get some package from hubs of repositories.
	.
	.
	.
	*/

}
